// const jwt = require("jsonwebtoken");
// const { statusCodes, messages } = require("../utils/helper");
// ["ADMIN","SUPER_ADMIN"]
// const roleGuard = (allowedRoles) => {
//   try {
//     // fetch details of user
//     const user = 
    
//     // check if the role of requested user present in allowed roles
//     // USER ROLE EXIST IN ABOVE ARRAY  

//     // IF EXISTS CALL NEXT()
//     // ELSE THROW { STATUS_CODES: 403, MESSAGE : }
//   } catch (error) {
//     let status = error.status ?? statusCodes
//     res.status(status).send()
//   }
// }


// module.exports = {createGuard};
